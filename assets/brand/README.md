# Brand asset note

NIQ's official Media Center exposes a Brandfolder logo pack:
https://nielseniq.com/global/en/about-us/niq-media-center/

The current execution environment could identify the official download endpoint but could not retrieve the binary ZIP for a local, unmodified commit. The campaign therefore does not redraw, trace, recolor or fabricate the NIQ mark.

Development uses a typeset `NIQ` company identifier. Before external protected release, add an official logo or wordmark from NIQ's Media Center here and replace the typeset identifier in `index.html`.
